[project]
name = "aurumai"
version = "0.0.1"
dependencies = [
    "fredapi",
    "numpy",
    "pandas",
    "python-dotenv",
    "yfinance",
]

[project.optional-dependencies]
dev = [
    "pytest",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
